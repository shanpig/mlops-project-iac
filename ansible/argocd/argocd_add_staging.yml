---
- name: Deploy proj10 Staging via ArgoCD & Helm
  hosts: node1
  become: yes
  vars:
    app_name: proj10-staging
    repo_url: https://github.com/shanpig/mlops-project-iac.git
    app_path: k8s/staging
    argocd_namespace: argocd
    app_port: 8082

  tasks:

    - name: Get ArgoCD admin password
      command: >
        kubectl get secret -n {{ argocd_namespace }} argocd-initial-admin-secret \
        -o jsonpath="{.data.password}"
      register: argocd_password_b64

    - name: Decode ArgoCD password
      shell: echo {{ argocd_password_b64.stdout }} | base64 --decode
      register: argocd_password

    - name: Login to ArgoCD
      command: >
        argocd login localhost --username admin \
        --password "{{ argocd_password.stdout }}" \
        --grpc-web --port-forward --port-forward-namespace={{ argocd_namespace }}
      changed_when: false

    - name: Read floating IP from Terraform output (controller-side)
      delegate_to: localhost
      become: false
      command: terraform -chdir=../tf/kvm output -raw floating_ip_out
      register: tf_floating_ip
      failed_when: false
      changed_when: false

    - name: Detect fallback external IP starting with 10.56
      set_fact:
        external_ip_fallback: "{{ ansible_all_ipv4_addresses | select('match', '^10\\.56\\..*') | list | first }}"

    - name: Select external IP for service exposure and app URLs
      set_fact:
        external_ip: "{{ (tf_floating_ip.stdout | default('') | trim) if (tf_floating_ip.rc is defined and tf_floating_ip.rc == 0 and (tf_floating_ip.stdout | trim) != '') else external_ip_fallback }}"
        app_public_url: "http://{{ (tf_floating_ip.stdout | default('') | trim) if (tf_floating_ip.rc is defined and tf_floating_ip.rc == 0 and (tf_floating_ip.stdout | trim) != '') else external_ip_fallback }}:{{ app_port }}"

    - name: Create proj10-staging namespace if missing
      command: kubectl create namespace proj10-staging
      register: ns_create
      failed_when: ns_create.rc != 0 and "AlreadyExists" not in ns_create.stderr
      changed_when: "'created' in ns_create.stdout"

    - name: Apply staging SealedSecret from Git
      command: >
        kubectl apply -f
        https://raw.githubusercontent.com/shanpig/mlops-project-iac/main/k8s/secrets/proj10-staging-proj10-adventurelog-secrets-sealedsecret.yaml

    - name: Check if ArgoCD app exists
      command: argocd app get {{ app_name }} --port-forward --port-forward-namespace={{ argocd_namespace }}
      register: app_check
      failed_when: false
      changed_when: false

    - name: Create ArgoCD Helm app if not exists
      when: app_check.rc != 0
      command: >
        argocd app create {{ app_name }} \
        --repo {{ repo_url }} \
        --path {{ app_path }} \
        --dest-server https://kubernetes.default.svc \
        --dest-namespace "" \
        --helm-set-string service.externalIP={{ external_ip }} \
        --helm-set-string app.publicUrl={{ app_public_url }} \
        --helm-set-string app.frontendUrl={{ app_public_url }} \
        --helm-set-string app.csrfTrustedOrigins={{ app_public_url }} \
        --sync-policy automated --self-heal \
        --port-forward --port-forward-namespace={{ argocd_namespace }}

    - name: Update ArgoCD Helm app if exists
      when: app_check.rc == 0
      command: >
        argocd app set {{ app_name }} \
        --helm-set-string service.externalIP={{ external_ip }} \
        --helm-set-string app.publicUrl={{ app_public_url }} \
        --helm-set-string app.frontendUrl={{ app_public_url }} \
        --helm-set-string app.csrfTrustedOrigins={{ app_public_url }} \
        --port-forward --port-forward-namespace={{ argocd_namespace }}

    - name: Display ArgoCD app status
      command: argocd app get {{ app_name }} --port-forward --port-forward-namespace={{ argocd_namespace }}
      register: final_status

    - name: Show final status
      debug:
        var: final_status.stdout
