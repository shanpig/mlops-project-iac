global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/prometheus-rules.yml

alerting:
  alertmanagers:
    - static_configs:
        - targets: ['alertmanager:9093']

scrape_configs:
  # ml-api (FastAPI) — prometheus-fastapi-instrumentator exposes /metrics on :8000
  - job_name: 'ml-api'
    static_configs:
      - targets: ['ml-api:8000']

  # stream-consumer — prometheus_client.start_http_server on :9100
  - job_name: 'stream-consumer'
    static_configs:
      - targets: ['stream-consumer-metrics:9100']

  # Pushgateway — receives batch-job metrics pushed by etl-job and batch-pipeline
  - job_name: 'pushgateway'
    honor_labels: true
    static_configs:
      - targets: ['pushgateway:9091']

  # GemSpot serving service metrics (model-serving)
  - job_name: 'gemspot-serving'
    metrics_path: /metrics
    static_configs:
      - targets: ['ml-serving:8000']

  # GemSpot app-level feedback metrics exposed by Django app (staging)
  - job_name: 'gemspot-app'
    metrics_path: /api/gemspot/metrics/
    static_configs:
      - targets: ['proj10-backend.proj10-staging.svc.cluster.local:80']

  # Self-scrape for Prometheus health and capacity
  - job_name: 'prometheus'
    static_configs:
      - targets: ['prometheus:9090']
