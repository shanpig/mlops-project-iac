apiVersion: v1
kind: ConfigMap
metadata:
  name: proj10-pipeline-phase-marker
  namespace: {{ .Values.namespace }}
data:
  phase: {{ .Values.pipeline.phase | quote }}
  description: "Current pipeline rollout marker. Services are introduced incrementally by phase."
